# Autotest MSXML XSLT

This is a template script you can use as a base for automated MSXML XSLT testing on Windows. This script isn't designed to work on Linux/MacOS.

## Prerequisites

I'm assuming that `msxml6.dll` exists in your `C:\Windows\System32\`.
